services:
  taiwan-address:
    build:
      context: ./service
    env_file:
      - runtime-settings.conf
    ports:
      - "127.0.0.1:${TWADDR_LOCAL_PORT:-18118}:8000"
    restart: unless-stopped
